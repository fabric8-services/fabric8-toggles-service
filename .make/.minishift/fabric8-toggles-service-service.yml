apiVersion: v1
kind: Service
metadata:
  name: fabric8-toggles-service
  namespace: f8toggles
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    deploymentconfig: fabric8-toggles-service
